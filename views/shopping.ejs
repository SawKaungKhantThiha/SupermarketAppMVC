<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Font: Montserrat (Gotham-like) -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App - Shopping</title>
  <style>
    /* Keep layout consistent with inventory.ejs and index.ejs */
    body {
      background-image: url('/images/bg.png');
      background-size: cover;
      background-position: center center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      background-color: #111213;
      color: #fff;
      font-family: 'Montserrat', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      pointer-events: none;
      z-index: 0;
    }
    .custom-navbar {
      background-color: transparent !important;
      padding-top: 1.05rem;
      padding-bottom: 1.05rem;
      min-height: 80px;
      z-index: 1030;
      box-shadow: none;
    }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing: -0.01em; }

    .inventory-grid { margin-top: 1.5rem; }
    .inventory-actions { margin-top: 1rem; }
    .search-box {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.14);
      border-radius: 999px;
      -webkit-backdrop-filter: blur(12px);
      backdrop-filter: blur(12px);
      padding: 0.65rem 1rem;
      color: #fff;
      box-shadow: 0 10px 28px rgba(0,0,0,0.35);
    }
    .search-box::placeholder { color: rgba(255,255,255,0.6); }
    .search-wrap { position: relative; }
    .filter-wrap { position: relative; }
    .filter-select {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 999px;
      -webkit-backdrop-filter: blur(10px);
      backdrop-filter: blur(10px);
      color: #fff;
      padding: 0.65rem 1rem;
      font-weight: 600;
      letter-spacing: 0.01em;
      appearance: none;
      background-image:
        linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0.02)),
        url("data:image/svg+xml,%3Csvg width='18' height='12' viewBox='0 0 18 12' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M2 4l7 7 7-7' stroke='%23ffffff' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
      background-repeat: no-repeat, no-repeat;
      background-position: 0 0, calc(100% - 0.9rem) center;
      background-size: auto, 18px 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.35);
    }
    .filter-select:focus {
      border-color: rgba(255,255,255,0.28);
      outline: none;
      box-shadow: 0 0 0 3px rgba(255,255,255,0.14);
    }
    .filter-select option { background: #0f1011; color: #fff; }

    .product-card {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.06);
      border-radius: 0.8rem;
      -webkit-backdrop-filter: blur(12px);
      backdrop-filter: blur(12px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.35);
      transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
    }
    .product-card:hover {
      transform: translateY(-4px);
      border-color: rgba(255,255,255,0.12);
      box-shadow: 0 16px 38px rgba(0,0,0,0.4);
    }
    .product-thumb {
      width: 78px;
      height: 78px;
      object-fit: cover;
      border-radius: 0.6rem;
      border: 1px solid rgba(255,255,255,0.08);
      box-shadow: inset 0 0 0 1px rgba(255,255,255,0.04);
    }
    .product-name {
      font-weight: 700;
      color: #fff;
      letter-spacing: -0.01em;
      text-decoration: none;
    }
    .product-name:hover { color: #f8f9fa; }
    .product-price {
      color: #ffffff;
      font-weight: 700;
      margin-top: 0.15rem;
    }
    .product-meta {
      font-size: 0.85rem;
      color: rgba(255,255,255,0.75);
    }
    .badge-quantity {
      background: rgba(255,255,255,0.08);
      color: #fff;
      font-weight: 700;
      letter-spacing: 0.02em;
      border-radius: 999px;
      padding: 0.35rem 0.65rem;
    }
    .category-pill {
      display: inline-flex;
      align-items: center;
      gap: 0.3rem;
      background: rgba(255,255,255,0.08);
      border: 1px solid rgba(255,255,255,0.12);
      color: #fff;
      border-radius: 999px;
      padding: 0.25rem 0.65rem;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.02em;
      text-transform: uppercase;
    }
    .card-actions .btn {
      flex: 1 1 auto;
    }

    .brand-logo {
      font-family: 'Montserrat', system-ui, sans-serif;
      display: inline-flex;
      flex-direction: column;
      text-align: center;
      text-decoration: none;
      line-height: 1.05;
      padding-left: 0.1rem;
    }

    .brand-main {
      font-size: 1.45rem;
      font-weight: 700;
      letter-spacing: 0.33em;
      text-transform: uppercase;
      color: #ffffff;
    }

    .brand-sub {
      font-size: 0.68rem;
      font-weight: 500;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.75);
      margin-top: 0.15rem;
    }

    footer .small { color: #e9ecef; }

    /* Large, dark footer */
    footer.site-footer {
      background: #0b0c0d;
      color: #fff;
      padding-top: 2.5rem;
      padding-bottom: 2.5rem;
      margin-top: 0;
      border-top: 1px solid rgba(255,255,255,0.03);
      z-index: 1025;
    }

    a, .nav-link { color: rgba(255,255,255,0.95); }

    .quantity-input-group {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background: rgba(255,255,255,0.08);
      border-radius: 0.4rem;
      padding: 0.3rem;
      border: 1px solid rgba(255,255,255,0.1);
    }

    .quantity-input-group button {
      width: 32px;
      height: 32px;
      padding: 0;
      border: none;
      background: transparent;
      color: #fff;
      font-weight: 600;
      cursor: pointer;
      border-radius: 0.3rem;
      transition: background 160ms ease;
      font-size: 1.1rem;
    }

    .quantity-input-group button:hover {
      background: rgba(255,255,255,0.15);
    }

    .quantity-input-group input {
      width: 50px;
      text-align: center;
      border: none;
      background: transparent;
      color: #fff;
      font-weight: 600;
      font-size: 1rem;
      padding: 0.25rem;
    }

    .quantity-input-group input:focus {
      outline: none;
    }

    /* Toast notification styles */
    .toast-container {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 2000;
    }

    .toast-notification {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(8px);
      color: #000000;
      padding: 1rem 1.5rem;
      border-radius: 0.5rem;
      box-shadow: 0 8px 24px rgba(0,0,0,0.4);
      margin-bottom: 0.75rem;
      animation: slideIn 300ms ease-out, slideOut 300ms ease-out 2700ms forwards;
      border: 1px solid rgba(255,255,255,0.1);
      font-weight: 500;
    }

    @keyframes slideIn {
      from {
        transform: translateX(400px);
        opacity: 0;
      }
      to {
        transform: translateX(0);
        opacity: 1;
      }
    }

    @keyframes slideOut {
      from {
        transform: translateX(0);
        opacity: 1;
      }
      to {
        transform: translateX(400px);
        opacity: 0;
      }
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
  <div class="toast-container" id="toastContainer"></div>

  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid" role="navigation" aria-label="Main navigation">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">SUPERMARKET</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar" aria-controls="collapsibleNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3">
            <span class="text-light small">Signed in as <strong><%= user.username %></strong></span>
          </li>
          <li class="nav-item me-2">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/orders">My Orders</a>
          </li>
          <li class="nav-item me-2">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/cart">View Cart</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout" role="button" title="Sign out">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <a class="visually-hidden-focusable" href="#main-content">Skip to main content</a>

  <main id="main-content" class="container flex-grow-1 py-4" role="main">
    <div class="inventory-actions mb-3">
      <div class="row g-2 align-items-center">
        <div class="col-12 col-md-6 col-lg-4">
          <input id="productSearch" type="search" class="form-control form-control-sm search-box" placeholder="Search products..." aria-label="Search products">
        </div>
        <div class="col-12 col-md-4 col-lg-3">
          <select id="categoryFilter" class="form-select form-select-sm filter-select" aria-label="Filter by category">
            <option value="">All categories</option>
          </select>
        </div>
      </div>
    </div>

    <div class="inventory-grid">
      <div class="row g-3 g-md-4">
        <% if (products && products.length) { %>
          <% for(let i=0; i < products.length; i++) { %>
            <div class="col-12 col-sm-6 col-lg-4 col-xl-3 product-card-wrapper" data-search="<%= (products[i].productName + ' ' + (products[i].category || '')).toLowerCase() %>" data-category="<%= (products[i].category || '').toLowerCase() %>">
              <article class="product-card h-100 d-flex flex-column p-3">
                <div class="d-flex align-items-start mb-3">
                  <img class="product-thumb me-3" src="/images/<%= products[i].image %>" alt="<%= products[i].productName %>" />
                  <div>
                    <a class="product-name d-block" href="/product/<%= products[i].id %>"><%= products[i].productName %></a>
                    <% if (products[i].category) { %>
                      <span class="category-pill mt-1"><%= products[i].category %></span>
                    <% } %>
                    <div class="product-price">$<%= products[i].price.toFixed(2) %></div>
                  </div>
                </div>
                <div class="d-flex align-items-center justify-content-between mb-3">
                  <span class="text-uppercase fw-semibold text-white-50 small">Quantity</span>
                  <div class="quantity-input-group">
                    <button type="button" onclick="decrementQty('quantity-<%= products[i].id %>')">−</button>
                    <input type="number" id="quantity-<%= products[i].id %>" name="quantity" value="1" min="1" readonly>
                    <button type="button" onclick="incrementQty('quantity-<%= products[i].id %>')">+</button>
                  </div>
                </div>
                <div class="card-actions mt-auto d-flex gap-2">
                  <form class="flex-grow-1" action="/add-to-cart/<%= products[i].id %>" method="POST" onsubmit="document.getElementById('qty-<%= products[i].id %>').value = document.getElementById('quantity-<%= products[i].id %>').value">
                    <input type="hidden" name="quantity" id="qty-<%= products[i].id %>" value="1">
                    <button type="submit" class="btn btn-outline-light btn-sm w-100">Buy</button>
                  </form>
                  <button type="button" class="btn btn-outline-light btn-sm w-100" onclick="addToCartWithNotification('<%= products[i].id %>', '<%= products[i].productName %>')">Add to Cart</button>
                </div>
              </article>
            </div>
          <% } %>
        <% } else { %>
          <div class="col-12">
            <div class="text-center text-white-50 py-5">
              <p class="mb-1 fw-semibold">No products found</p>
              <small class="text-white-50">Browse again later.</small>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </main>

  <script>
    // Quantity controls
    function incrementQty(id) {
      const input = document.getElementById(id);
      if (!input) return;
      const current = parseInt(input.value, 10) || 1;
      input.value = current + 1;
    }

    function decrementQty(id) {
      const input = document.getElementById(id);
      if (!input) return;
      const current = parseInt(input.value, 10) || 1;
      input.value = Math.max(1, current - 1);
    }

    // Simple toast notifications
    function showToast(message) {
      const container = document.getElementById('toastContainer');
      if (!container) return;
      const toast = document.createElement('div');
      toast.className = 'toast-notification';
      toast.textContent = message;
      container.appendChild(toast);
      setTimeout(() => toast.remove(), 3200);
    }

    // Add to cart via fetch so button feels responsive
    async function addToCartWithNotification(productId, productName) {
      const qtyInput = document.getElementById(`quantity-${productId}`);
      const quantity = qtyInput ? Math.max(1, parseInt(qtyInput.value, 10) || 1) : 1;
      try {
        const res = await fetch(`/add-to-cart/${productId}`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: `quantity=${encodeURIComponent(quantity)}`
        });
        if (!res.ok) throw new Error('Request failed');
        showToast(`${productName} (x${quantity}) added to cart!`);
      } catch (err) {
        console.error(err);
        showToast('Could not add to cart. Please try again.');
      }
    }

    // Client-side filter for product cards by name and category
    (function() {
      const searchInput = document.getElementById('productSearch');
      const categorySelect = document.getElementById('categoryFilter');
      if (!searchInput || !categorySelect) return;

      const cards = Array.from(document.querySelectorAll('.product-card-wrapper'));

      const categories = new Set();
      cards.forEach(card => {
        const cat = (card.dataset.category || '').trim();
        if (cat) categories.add(cat);
      });
      Array.from(categories).sort().forEach(cat => {
        const opt = document.createElement('option');
        opt.value = cat;
        opt.textContent = cat.charAt(0).toUpperCase() + cat.slice(1);
        categorySelect.appendChild(opt);
      });

      function filterCards() {
        const term = searchInput.value.trim().toLowerCase();
        const cat = categorySelect.value.trim();
        cards.forEach(card => {
          const termSpace = card.dataset.search || '';
          const cardCat = card.dataset.category || '';
          const matchesTerm = termSpace.includes(term);
          const matchesCategory = !cat || cardCat === cat;
          card.style.display = (matchesTerm && matchesCategory) ? '' : 'none';
        });
      }

      searchInput.addEventListener('input', filterCards);
      categorySelect.addEventListener('change', filterCards);
    })();
  </script>


<footer class="site-footer mt-auto">
  <div class="container text-center">
    <p class="mb-1">Supermarket Singapore elevates everyday shopping with curated essentials, premium quality, and a refined digital experience. Seamless, stylish, and sophisticated — where everyday living meets modern luxury.</p>
    <small class="small">© <%= new Date().getFullYear() %> — by Saw Kaung Khant Thiha.</small>
  </div>
</footer>
</body>
</html>



