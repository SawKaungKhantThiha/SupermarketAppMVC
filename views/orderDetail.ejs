<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Order #<%= order.id %></title>
  <style>
    body { background:#111213; color:#fff; font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif; background-image:url('/images/bg.png'); background-size:cover; background-repeat:no-repeat; background-attachment:fixed; }
    body::before { content:''; position:fixed; inset:0; background:rgba(0,0,0,0.45); z-index:-1; }
    .custom-navbar { background-color:transparent !important; padding-top:1.05rem; padding-bottom:1.05rem; min-height:80px; z-index:1030; box-shadow:none; }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing:-0.01em; }
    .card-frosted { background:rgba(255,255,255,0.05); border:1px solid rgba(255,255,255,0.08); border-radius:0.8rem; -webkit-backdrop-filter:blur(12px); backdrop-filter:blur(12px); box-shadow:0 12px 34px rgba(0,0,0,0.45); padding:1.2rem; }
    .brand-logo { font-family:'Montserrat',system-ui,sans-serif; display:inline-flex; flex-direction:column; text-align:center; text-decoration:none; line-height:1.05; padding-left:0.1rem; }
    .brand-main { font-size:1.45rem; font-weight:700; letter-spacing:0.33em; text-transform:uppercase; color:#ffffff; }
    .brand-sub { font-size:0.68rem; font-weight:500; letter-spacing:0.28em; text-transform:uppercase; color:rgba(255,255,255,0.75); margin-top:0.15rem; }
    footer.site-footer { background:#0b0c0d; color:#fff; padding:2.5rem 0; border-top:1px solid rgba(255,255,255,0.03); }
    a, .nav-link { color: rgba(255,255,255,0.95); }
    /* Receipt tweaks */
    .receipt-header { border-bottom: 1px dashed rgba(255,255,255,0.25); padding-bottom: 0.75rem; margin-bottom: 1rem; }
    .receipt-meta small { color: rgba(255,255,255,0.7); }
    .receipt-thanks { border-top: 1px dashed rgba(255,255,255,0.25); padding-top: 0.75rem; margin-top: 1rem; color: rgba(255,255,255,0.8); }

    /* Print-friendly adjustments */
    @media print {
      nav, footer, .no-print { display: none !important; }
      body { background: #fff; color: #000; }
      .card-frosted { box-shadow: none; border: 1px dashed #777; background: #fff; color: #000; }
      table { color: #000 !important; }
      .table.table-dark { color: #000; --bs-table-bg: #fff; }
      .table th, .table td { border-color: #ccc !important; }
      a { color: #000 !important; text-decoration: none; }
    }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">SUPERMARKET</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3"><span class="text-light small">Signed in as <strong><%= user.username %></strong></span></li>
          <li class="nav-item"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/orders">Back to Orders</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container flex-grow-1 py-4">
    <div class="card-frosted">
      <div class="receipt-header">
        <div class="d-flex justify-content-between align-items-center">
          <div>
            <h4 class="fw-bold mb-0">Order #<%= order.id %></h4>
            <div class="receipt-meta mt-1">
              <small><%= new Date(order.createdAt).toLocaleString() %></small><br>
              <small>Supermarket Singapore</small>
            </div>
          </div>
          <div class="text-end">
            <div class="text-white-50 small">Total</div>
            <div class="fs-5 fw-bold">$<%= Number(order.total).toFixed(2) %></div>
          </div>
        </div>
      </div>
      <% if (user) { %>
        <div class="mb-3">
          <div class="text-white-50 small">Billed To</div>
          <div class="fw-semibold"><%= user.username %></div>
          <div class="text-white-50 small"><%= user.email || '' %></div>
        </div>
      <% } %>
      <% const paymentInfoSafe = (typeof paymentInfo !== 'undefined') ? paymentInfo : null; %>
      <div class="mb-3">
        <div class="text-white-50 small">Payment</div>
        <% if (paymentInfoSafe) { %>
          <div class="fw-semibold"><%= paymentInfoSafe.method || 'Payment captured' %></div>
          <% if (paymentInfoSafe.cardName) { %>
            <div class="text-white-50 small">Name: <%= paymentInfoSafe.cardName %></div>
          <% } %>
          <% if (paymentInfoSafe.cardLast4) { %>
            <div class="text-white-50 small">Card ending •••• <%= paymentInfoSafe.cardLast4 %></div>
          <% } %>
        <% } else { %>
          <div class="text-white-50 small">Payment details not captured (demo checkout).</div>
        <% } %>
      </div>
      <% if (order.address) { %>
        <div class="mb-3">
          <div class="text-white-50 small">Delivery Address</div>
          <div class="fw-semibold"><%= order.address %></div>
        </div>
      <% } %>
      <div class="d-flex gap-2 mb-3 no-print">
        <button class="btn btn-outline-light btn-sm" onclick="window.print()">Print / Save as PDF</button>
        <a class="btn btn-outline-light btn-sm" href="/orders">Back to Orders</a>
      </div>

      <div class="table-responsive">
        <table class="table table-dark table-hover align-middle mb-0">
          <thead>
            <tr>
              <th>Product</th>
              <th>Category</th>
              <th>Price</th>
              <th>Quantity</th>
              <th>Subtotal</th>
            </tr>
          </thead>
          <tbody>
            <% items.forEach(item => { %>
              <tr>
                <td><%= item.productName %></td>
                <td><%= item.category || '—' %></td>
                <td>$<%= Number(item.price).toFixed(2) %></td>
                <td><%= item.quantity %></td>
                <td>$<%= (Number(item.price) * item.quantity).toFixed(2) %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
      <div class="receipt-thanks">
        <small>Thank you for shopping with us!</small>
      </div>
    </div>
  </main>  <footer class="site-footer mt-auto">
  <div class="container text-center">
    <p class="mb-1">Supermarket Singapore elevates everyday shopping with curated essentials, premium quality, and a refined digital experience. Seamless, stylish, and sophisticated — where everyday living meets modern luxury.</p>
    <small class="small">© <%= new Date().getFullYear() %> — by Saw Kaung Khant Thiha.</small>
  </div>
</footer>
</body>
</html>

