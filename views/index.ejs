<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Font: Montserrat (Gotham-like) -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App</title>
  <style>
    /* Page background image (put image at public/images/bg.png). Uses a stronger overlay for white text readability. */
    body {
      background-image: url('/images/bg.png');
      background-size: cover;
      background-position: center center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      /* fallback background color */
      background-color: #111213;
      color: #fff; /* default white text site-wide */
    }
    /* Add a stronger dark overlay so white text stays readable on the page */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      pointer-events: none;
      z-index: -1;
    }
    /* Invisible (transparent) navbar that still sits above overlay and keeps spacing */
    .custom-navbar {
      background-color: transparent !important;
      padding-top: 1.05rem;
      padding-bottom: 1.05rem;
      min-height: 80px;
      z-index: 1030; /* above the background overlay */
      box-shadow: none;
    }
    .custom-navbar .navbar-brand {
      font-size: 1.45rem;
      font-weight: 700;
      letter-spacing: -0.01em;
    }
    .custom-navbar .nav-link {
      font-size: 1rem;
      padding-left: 0.6rem;
      padding-right: 0.6rem;
    }
    .custom-navbar .btn {
      padding: 0.36rem 0.7rem;
    }
    /* Apply Montserrat (Gotham-like) as the primary typeface */
    body {
      font-family: 'Montserrat', 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }
    h1, h2, h3, h4, h5, h6 {
      font-weight: 600;
      letter-spacing: -0.01em;
    }
    /* Small, local styles to keep the homepage friendly and clean */
    /* Visible skip link when focused for keyboard users */
    .visually-hidden-focusable {
      position: absolute;
      left: -10000px;
      top: auto;
      width: 1px;
      height: 1px;
      overflow: hidden;
    }
    .visually-hidden-focusable:focus,
    .visually-hidden-focusable:active {
      position: static;
      left: auto;
      width: auto;
      height: auto;
      margin: 0.5rem;
      padding: 0.5rem 1rem;
      background: #0d6efd;
      color: #fff;
      z-index: 1000;
      border-radius: 0.25rem;
    }
    .hero {
      margin-top: 1rem;
      padding: 2rem;
      border-radius: 0.6rem;
      color: #fff;
      text-align: center; /* center-align main content */
    }
  .lead-cta { font-size: 1.05rem; color: rgba(255,255,255,0.95); }
    /* Minimalistic button styles used across the page */
    .btn-cta {
      min-width: 0;
      padding: 0.38rem 0.75rem;
      border-radius: 0.35rem;
      font-weight: 500;
      box-shadow: none;
    }
    .btn-minimal {
      box-shadow: none !important;
      background-image: none !important;
    }
    
    footer .small { color: #e9ecef; }

    /* Large, dark footer */
    footer.site-footer {
      background: #0b0c0d;
      color: #fff;
      padding-top: 2.5rem;
      padding-bottom: 2.5rem;
      margin-top: 0;
      border-top: 1px solid rgba(255,255,255,0.03);
      z-index: 1025;
    }

    /* Slightly stronger focus ring for interactive elements */
    .btn:focus, a:focus {
      box-shadow: 0 0 0 0.18rem rgba(255,255,255,0.12);
      outline: none;
    }

    .brand-logo {
      font-family: 'Montserrat', system-ui, sans-serif;
      display: inline-flex;
      flex-direction: column;
      text-align: center;
      text-decoration: none;
      line-height: 1.05;
      padding-left: 0.1rem;
    }

    .brand-main {
      font-size: 1.45rem;
      font-weight: 700;
      letter-spacing: 0.33em;
      text-transform: uppercase;
      color: #ffffff;
    }

    .brand-sub {
      font-size: 0.68rem;
      font-weight: 500;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.75);
      margin-top: 0.15rem;
    }

    /* Links default to lighter white for contrast */
    a { color: #ffffff; }
    a.btn, .nav-link { color: rgba(255,255,255,0.95); }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
  <a class="visually-hidden-focusable" href="#main-content">Skip to main content</a>
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid" role="navigation" aria-label="Main navigation">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">SUPERMARKET</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar" aria-controls="collapsibleNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <% if (!user || user.role !== 'admin') { %>
        <ul class="navbar-nav me-auto mb-2 mb-sm-0">
          <li class="nav-item">
            <a class="nav-link" href="/shopping" title="Browse products">Shop</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart" title="View your cart">Cart</a>
          </li>
        </ul>
        <% } %>

        <ul class="navbar-nav ms-auto">
          <% if (!user) { %>
            <li class="nav-item me-2">
              <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/register" role="button" title="Create an account">Register</a>
            </li>
            <li class="nav-item">
              <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/login" role="button" title="Sign in to your account">Login</a>
            </li>
          <% } else { %>
            <li class="nav-item d-flex align-items-center me-3">
              <span class="text-light small">Signed in as <strong><%= user.username %></strong></span>
            </li>
            <li class="nav-item ms-2">
              <a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout" role="button" title="Sign out">Logout</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

    <main id="main-content" class="container flex-grow-1 py-4" role="main">
    <div class="row align-items-center justify-content-center" style="min-height:70vh;"><div class="col-12 col-lg-10 text-center">
        <section class="hero w-100">
          <div class="row align-items-center">
            <div class="col-12">
              <h1 class="display-6">Welcome to Supermarket Singapore</h1>
              <p class="lead lead-cta">Quick, simple shopping for everyday essentials — all in a few clicks.</p>

              <div class="mt-3">
                <% if (!user) { %>
                  <!-- Use light buttons inside transparent hero so they stand out -->
                  <a class="btn btn-outline-light btn-cta" href="/register" role="button" title="Create an account">Create an account</a>
                  <a class="btn btn-outline-light btn-cta" href="/login" role="button" title="Login to your account">Login</a>
                <% } else { %>
                  <% if (user.role === 'admin') { %>
                    <a class="btn btn-outline-light btn-cta" href="/inventory" role="button" title="Go to inventory">Manage Inventory</a>
                    <a class="btn btn-outline-light btn-cta ms-2" href="/admin/users" role="button" title="Manage users and orders">Manage Users</a>
                  <% } else { %>
                    <a class="btn btn-outline-light btn-cta" href="/shopping" role="button" title="Browse items">Browse All Items</a>
                    <a class="btn btn-outline-light btn-cta ms-2" href="/orders" role="button" title="View your orders">My Orders</a>
                  <% } %>
                <% } %>
              </div>
            </div>
          </div>
        </section>
      </div>
    </div>
  </main>  <footer class="site-footer mt-auto">
  <div class="container text-center">
    <p class="mb-1">Supermarket Singapore elevates everyday shopping with curated essentials, premium quality, and a refined digital experience. Seamless, stylish, and sophisticated — where everyday living meets modern luxury.</p>
    <small class="small">© <%= new Date().getFullYear() %> — by Saw Kaung Khant Thiha.</small>
  </div>
</footer>
</body>
</html>

